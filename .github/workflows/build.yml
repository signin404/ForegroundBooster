name: Build C++ Process Controller

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Compile and Embed Manifest
      run: |
        call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvarsall.bat" x64
        
        rem --- FIX: 添加 /D UNICODE 和 /D _UNICODE 标志来强制使用 Unicode 编译 ---
        cl.exe /EHsc /O2 /W3 /D UNICODE /D _UNICODE /Fe:ProcessController.exe ProcessController.cpp kernel32.lib user32.lib psapi.lib /link /SUBSYSTEM:CONSOLE
        
        mt.exe -manifest ProcessController.exe.manifest -outputresource:ProcessController.exe;1
      shell: cmd

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: ProcessController
        path: ProcessController.exe
