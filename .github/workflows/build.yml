name: Build C++ Process Controller

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Compile and Embed Manifest
      run: |
        call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvarsall.bat" x64
        
        rem --- FIX: 添加 /utf-8 标志，确保编译器正确处理源文件中的中文字符 ---
        cl.exe /EHsc /O2 /W3 /utf-8 /Fe:ProcessController.exe ProcessController.cpp /link /SUBSYSTEM:CONSOLE
        
        mt.exe -manifest ProcessController.exe.manifest -outputresource:ProcessController.exe;1
      shell: cmd

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: ProcessController
        path: ProcessController.exe
